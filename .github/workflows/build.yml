name: Build and Release APKs

# 同時に複数回このワークフローが走ると、download-tools ジョブが同じ
# キャッシュキーを取り合って "Unable to reserve cache" 警告が出る
# （実害は小さいが、無駄な再ダウンロードや紛らわしいログの原因になる）。
# 新しい実行が来たら前の実行をキャンセルして直列化する。
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      source_tags:
        description: 'JSON object of source names to exact patch Release tags'
        required: false
        default: '{}'
      morphe_updated:
        description: 'Whether Morphe (Official) was updated'
        required: false
        default: 'false'
      anddea_updated:
        description: 'Whether Anddea was updated'
        required: false
        default: 'false'
      rushiranpise_updated:
        description: 'Whether rushiranpise was updated'
        required: false
        default: 'false'
      rookie_updated:
        description: 'Whether RookieEnough was updated'
        required: false
        default: 'false'
      tosox_updated:
        description: 'Whether Tosox was updated'
        required: false
        default: 'false'
      yuzu_updated:
        description: 'Whether YuzuMikan404 was updated'
        required: false
        default: 'false'
      dropped_updated:
        description: 'Whether Dropped-Patches was updated'
        required: false
        default: 'false'
      morphe_force_build:
        description: 'Force build Morphe due to APK update (no NEW badge)'
        required: false
        default: 'false'
      anddea_force_build:
        description: 'Force build Anddea due to APK update (no NEW badge)'
        required: false
        default: 'false'
      rushiranpise_force_build:
        description: 'Force build rushiranpise due to APK update (no NEW badge)'
        required: false
        default: 'false'
      rookie_force_build:
        description: 'Force build RookieEnough due to APK update (no NEW badge)'
        required: false
        default: 'false'
      tosox_force_build:
        description: 'Force build Tosox due to APK update (no NEW badge)'
        required: false
        default: 'false'
      yuzu_force_build:
        description: 'Force build YuzuMikan404 due to APK update (no NEW badge)'
        required: false
        default: 'false'
      dropped_force_build:
        description: 'Force build Dropped-Patches due to APK update (no NEW badge)'
        required: false
        default: 'false'
      apk_updated_apps:
        description: 'JSON array of apps whose APK was updated'
        required: false
        default: '[]'
      build_all_sources:
        description: 'Build every app/source entry in my-patch-config.json'
        required: false
        default: 'false'
      updated_sources:
        description: 'Comma-separated patch sources to build incrementally'
        required: false
        default: ''
      updated_apps:
        description: 'Comma-separated apps whose base APK was updated'
        required: false
        default: ''

jobs:
  download-tools:
    name: Download Build Tools
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      cache-key: ${{ steps.resolve-tags.outputs.cache-key }}
      resolved-tags: ${{ steps.resolve-tags.outputs.resolved_tags }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Validate Repository Configuration
        run: python3 scripts/validate_repository.py

      - name: Resolve Patch Tags and Compute Cache Key
        id: resolve-tags
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SOURCE_TAGS_JSON: ${{ github.event.inputs.source_tags }}
          CONFIG_HASH: ${{ hashFiles('my-patch-config.json', 'arch-config.json', 'sources/**') }}
        run: python3 scripts/resolve_build_tools.py

      - name: Restore Cached Tools
        id: cache-restore
        uses: actions/cache/restore@v6
        with:
          path: tools/
          key: ${{ steps.resolve-tags.outputs.cache-key }}

      - name: Setup Python
        if: steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/setup-python@v7
        with:
          python-version: 3.11

      - name: Download All Source Tools
        if: steps.cache-restore.outputs.cache-hit != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          UPD_MORPHE:   ${{ github.event.inputs.morphe_updated }}
          UPD_ANDDEA:   ${{ github.event.inputs.anddea_updated }}
          UPD_RUSHIRANPISE: ${{ github.event.inputs.rushiranpise_updated }}
          UPD_ROOKIE:   ${{ github.event.inputs.rookie_updated }}
          UPD_TOSOX:    ${{ github.event.inputs.tosox_updated }}
          UPD_YUZU:     ${{ github.event.inputs.yuzu_updated }}
          UPD_DROPPED:  ${{ github.event.inputs.dropped_updated }}
          SOURCE_TAGS_JSON:           ${{ steps.resolve-tags.outputs.resolved_tags }}
          PAT:                        ${{ secrets.PAT }}
        run: |
          pip install -r requirements.txt -q
          python3 scripts/download_all_tools.py

      - name: Save Cached Tools
        if: steps.cache-restore.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        with:
          path: tools/
          key: ${{ steps.resolve-tags.outputs.cache-key }}

  prepare-matrix:
    name: Prepare Build Matrix
    runs-on: ubuntu-latest
    needs: [download-tools]
    outputs:
      matrix: ${{ steps.read-matrix.outputs.matrix }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Read Patch Config and Filter by Updated Sources
        id: read-matrix
        env:
          UPD_MORPHE:   ${{ github.event.inputs.morphe_updated }}
          UPD_ANDDEA:   ${{ github.event.inputs.anddea_updated }}
          UPD_RUSHIRANPISE: ${{ github.event.inputs.rushiranpise_updated }}
          UPD_ROOKIE:   ${{ github.event.inputs.rookie_updated }}
          UPD_TOSOX:    ${{ github.event.inputs.tosox_updated }}
          UPD_YUZU:     ${{ github.event.inputs.yuzu_updated }}
          UPD_DROPPED:  ${{ github.event.inputs.dropped_updated }}
          FORCE_MORPHE:  ${{ github.event.inputs.morphe_force_build }}
          FORCE_ANDDEA:  ${{ github.event.inputs.anddea_force_build }}
          FORCE_RUSHIRANPISE: ${{ github.event.inputs.rushiranpise_force_build }}
          FORCE_ROOKIE:  ${{ github.event.inputs.rookie_force_build }}
          FORCE_TOSOX:   ${{ github.event.inputs.tosox_force_build }}
          FORCE_YUZU:    ${{ github.event.inputs.yuzu_force_build }}
          FORCE_DROPPED: ${{ github.event.inputs.dropped_force_build }}
          BUILD_ALL_SOURCES: ${{ github.event.inputs.build_all_sources }}
          UPDATED_SOURCES: ${{ github.event.inputs.updated_sources }}
          UPDATED_APPS: ${{ github.event.inputs.updated_apps }}
          APK_UPDATED_APPS: ${{ github.event.inputs.apk_updated_apps }}
        run: |
          python3 scripts/prepare_matrix.py

  download-apks:
    name: Download ${{ matrix.app_name }} base APK
    needs: [download-tools, prepare-matrix]
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
    continue-on-error: true
    strategy:
      fail-fast: false
      max-parallel: 8
      matrix:
        include: ${{ fromJson(needs.prepare-matrix.outputs.matrix) }}

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Restore Cached Tools
        uses: actions/cache/restore@v6
        with:
          path: tools/
          key: ${{ needs.download-tools.outputs.cache-key }}
          fail-on-cache-miss: true

      - name: Install Python
        uses: actions/setup-python@v7
        with:
          python-version: 3.11

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Install pinned Google Play clients
        run: python3 scripts/install_google_play_clients.py

      - name: Download Base APK
        id: download-apk-primary
        continue-on-error: true
        env:
          APP_NAME: ${{ matrix.app_name }}
          SOURCE: ${{ matrix.source }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VIRUSTOTAL_API_KEY: ${{ secrets.VIRUSTOTAL_API_KEY }}
          GPLAYDL_API_KEY: ${{ secrets.GPLAYDL_API_KEY }}
          GPLAY_EMAIL: ${{ secrets.GPLAY_EMAIL }}
          GPLAY_AAS_TOKEN: ${{ secrets.GPLAY_AAS_TOKEN }}
          GPLAYDL_PREFERRED_PROFILE: ${{ vars.GPLAYDL_PREFERRED_PROFILE }}
          GPLAY_AUTH_TOKEN: ${{ secrets.GPLAY_AUTH_TOKEN }}
          AURORA_DISPENSER_URL: ${{ vars.AURORA_DISPENSER_URL }}
          HTTP_RETRIES: '4'
          HTTP_RETRY_BASE_SECONDS: '5'
          HTTP_RETRY_MAX_SECONDS: '60'
          APK_VERSION_CANDIDATES: '5'
          APKMIRROR_REQUEST_INTERVAL_SECONDS: '3.5'
          BASE_APK_CACHE: 'true'
          BASE_APK_CACHE_TAG: 'base-apk-cache-v2'
          BASE_APK_CACHE_REPOSITORY: ${{ github.repository }}
        run: python3 scripts/run_logged.py --output download-primary.log -- python3 scripts/download_apks.py

      - name: Connect failed download to Japanese Tailscale exit node
        id: tailscale-fallback
        if: ${{ steps.download-apk-primary.outcome == 'failure' }}
        continue-on-error: true
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Select advertised Japanese Tailscale exit node
        id: select-tailscale-exit-node
        if: ${{ steps.download-apk-primary.outcome == 'failure' && steps.tailscale-fallback.outcome == 'success' }}
        continue-on-error: true
        env:
          TS_EXIT_NODE: ${{ vars.TS_EXIT_NODE }}
        run: |
          exit_node_ip="$(python3 scripts/resolve_tailscale_exit_node.py "$TS_EXIT_NODE")"
          sudo tailscale set --exit-node="$exit_node_ip"
          tailscale ping "$exit_node_ip"

      - name: Verify fallback egress is in Japan
        id: verify-japan-egress
        if: ${{ steps.select-tailscale-exit-node.outcome == 'success' }}
        continue-on-error: true
        run: python3 scripts/verify_japan_egress.py

      - name: Retry Base APK download through Japan
        id: download-apk-japan
        if: ${{ steps.verify-japan-egress.outcome == 'success' }}
        continue-on-error: true
        env:
          APP_NAME: ${{ matrix.app_name }}
          SOURCE: ${{ matrix.source }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VIRUSTOTAL_API_KEY: ${{ secrets.VIRUSTOTAL_API_KEY }}
          GPLAYDL_API_KEY: ${{ secrets.GPLAYDL_API_KEY }}
          GPLAY_EMAIL: ${{ secrets.GPLAY_EMAIL }}
          GPLAY_AAS_TOKEN: ${{ secrets.GPLAY_AAS_TOKEN }}
          GPLAYDL_PREFERRED_PROFILE: ${{ vars.GPLAYDL_PREFERRED_PROFILE }}
          GPLAY_AUTH_TOKEN: ${{ secrets.GPLAY_AUTH_TOKEN }}
          AURORA_DISPENSER_URL: ${{ vars.AURORA_DISPENSER_URL }}
          HTTP_RETRIES: '4'
          HTTP_RETRY_BASE_SECONDS: '5'
          HTTP_RETRY_MAX_SECONDS: '60'
          APK_VERSION_CANDIDATES: '5'
          APKMIRROR_REQUEST_INTERVAL_SECONDS: '3.5'
          BASE_APK_CACHE: 'true'
          BASE_APK_CACHE_TAG: 'base-apk-cache-v2'
          BASE_APK_CACHE_REPOSITORY: ${{ github.repository }}
        run: python3 scripts/run_logged.py --output download-japan.log -- python3 scripts/download_apks.py

      - name: Final provider fallback if Japan path did not succeed
        id: download-apk-provider-rescue
        if: ${{ always() && steps.download-apk-primary.outcome == 'failure' && steps.download-apk-japan.outcome != 'success' }}
        continue-on-error: true
        env:
          APP_NAME: ${{ matrix.app_name }}
          SOURCE: ${{ matrix.source }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HTTP_RETRIES: '3'
          HTTP_RETRY_BASE_SECONDS: '2'
          HTTP_RETRY_MAX_SECONDS: '20'
          APK_VERSION_CANDIDATES: '5'
          APKMIRROR_REQUEST_INTERVAL_SECONDS: '3.5'
          BASE_APK_CACHE: 'true'
          BASE_APK_CACHE_TAG: 'base-apk-cache-v2'
          BASE_APK_CACHE_REPOSITORY: ${{ github.repository }}
        run: python3 -m scripts.run_provider_rescue

      - name: Resolve Download Result
        id: download-apk
        if: ${{ always() }}
        env:
          PRIMARY_OUTCOME: ${{ steps.download-apk-primary.outcome }}
          JAPAN_OUTCOME: ${{ steps.download-apk-japan.outcome }}
          PROVIDER_RESCUE_OUTCOME: ${{ steps.download-apk-provider-rescue.outcome }}
        run: python3 scripts/resolve_download_result.py

      - name: Record Download Result
        if: always()
        env:
          APP_NAME: ${{ matrix.app_name }}
          SOURCE: ${{ matrix.source }}
          SOURCE_NAME: ${{ matrix.source_label }}
          DOWNLOAD_STATUS: ${{ steps.download-apk.outcome }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: python3 scripts/record_download_status.py

      - name: Upload Download Result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: download-status-${{ matrix.app_name }}-${{ matrix.source }}
          path: download-status/*.txt
          retention-days: 7

      - name: Upload Shared Base APK Input
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: base-input-${{ matrix.app_name }}-${{ matrix.source }}
          path: |
            base-apk-input/*
            build-metadata/apk-sources.json
          retention-days: 1

  scan-base-apks:
    name: Scan Base APKs with VirusTotal
    needs: [download-apks]
    if: ${{ !cancelled() && needs.download-apks.result != 'skipped' }}
    runs-on: ubuntu-latest
    timeout-minutes: 300
    permissions:
      contents: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: 3.11

      - name: Restore VirusTotal Hash Cache
        id: vt-cache-restore
        uses: actions/cache/restore@v6
        with:
          path: .cache/virustotal
          key: virustotal-hash-v1-${{ runner.os }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            virustotal-hash-v1-${{ runner.os }}-

      - name: Download Shared Base APK Inputs
        uses: actions/download-artifact@v8
        with:
          path: ./base-apks
          pattern: base-input-*
          merge-multiple: true

      - name: Scan Unmodified Base APKs with VirusTotal
        env:
          VIRUSTOTAL_API_KEY: ${{ secrets.VIRUSTOTAL_API_KEY }}
          VT_WORKERS: '32'
          VT_REQUEST_INTERVAL_SECONDS: '8'
          VT_POLL_INTERVAL_SECONDS: '30'
          VT_ANALYSIS_TIMEOUT_SECONDS: '2700'
          VT_MAX_RETRIES: '6'
          VT_MAX_ANALYSIS_AGE_DAYS: '90'
        run: |
          pip install -r requirements.txt
          python3 -m scripts.scan_virustotal \
            --directory ./base-apks \
            --cache ./.cache/virustotal/hash-results.json \
            --title "VirusTotal base APK scan results (before patching)" \
            --markdown ./virustotal_base_results.md \
            --json ./virustotal_base_results.json

      - name: Save VirusTotal Hash Cache
        if: always() && hashFiles('.cache/virustotal/hash-results.json') != ''
        uses: actions/cache/save@v6
        with:
          path: .cache/virustotal
          key: virustotal-hash-v1-${{ runner.os }}-${{ github.run_id }}-${{ github.run_attempt }}

      - name: Upload VirusTotal Report
        if: always() && hashFiles('virustotal_base_results.*') != ''
        uses: actions/upload-artifact@v7
        with:
          name: virustotal-report
          path: |
            virustotal_base_results.md
            virustotal_base_results.json
          retention-days: 30

  build-apps:
    name: Build ${{ matrix.app_name }} with ${{ matrix.source_label }}
    needs: [download-tools, prepare-matrix, download-apks]
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        include: ${{ fromJson(needs.prepare-matrix.outputs.matrix) }}
    continue-on-error: true

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Restore Cached Tools
        uses: actions/cache/restore@v6
        with:
          path: tools/
          key: ${{ needs.download-tools.outputs.cache-key }}
          fail-on-cache-miss: true

      - name: Install Python
        uses: actions/setup-python@v7
        with:
          python-version: 3.11

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: Install pinned Google Play clients
        run: python3 scripts/install_google_play_clients.py

      - name: Download Shared Base APK Input
        uses: actions/download-artifact@v8
        with:
          name: base-input-${{ matrix.app_name }}-${{ matrix.source }}
          path: .

      - name: Resolve Pre-downloaded APK
        run: python3 scripts/resolve_pre_downloaded_apk.py

      - name: Build APK
        id: build-apk
        env:
          APP_NAME: ${{ matrix.app_name }}
          SOURCE: ${{ matrix.source }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HTTP_RETRIES: '4'
          HTTP_RETRY_BASE_SECONDS: '5'
          HTTP_RETRY_MAX_SECONDS: '60'
          APK_VERSION_CANDIDATES: '5'
          APKMIRROR_REQUEST_INTERVAL_SECONDS: '3.5'
          BASE_APK_CACHE: 'true'
          BASE_APK_CACHE_TAG: 'base-apk-cache-v2'
          BASE_APK_CACHE_REPOSITORY: ${{ github.repository }}
        run: python3 scripts/run_logged.py --output build.log -- python3 -m src

      - name: Record Build Result
        if: always()
        env:
          APP_NAME: ${{ matrix.app_name }}
          SOURCE: ${{ matrix.source }}
          SOURCE_NAME: ${{ matrix.source_label }}
          BUILD_STATUS: ${{ steps.build-apk.outcome }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: python3 scripts/record_build_status.py

      - name: Upload Build Result
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: build-status-${{ matrix.app_name }}-${{ matrix.source }}
          path: build-status/*
          retention-days: 7

      - name: Upload APK Artifact
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: apk-${{ matrix.app_name }}-${{ matrix.source }}
          path: |
            *.apk
            build-metadata/apk-sources.json

  report-build-failures:
    name: Report Per-App Build Failures
    needs: [prepare-matrix, download-apks, build-apps]
    if: ${{ always() && needs.prepare-matrix.result == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: 3.11

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Download Build Failure Results
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          path: failure-results
          pattern: '*-status-*'
          merge-multiple: true

      - name: Create or Update App Issues
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: python3 scripts/report_build_failure.py --directory failure-results

  report-partial-patch-failures:
    name: Report Partial Patch Failures
    needs: [build-apps, create-release]
    if: ${{ needs.create-release.result == 'success' }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Download Build Results
        uses: actions/download-artifact@v8
        with:
          path: partial-results
          pattern: build-status-*
          merge-multiple: true

      - name: Create Partial Patch Failure Issues
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
        run: python3 scripts/report_build_failure.py --directory partial-results --partial-only

  update-base-apk-cache:
    name: Update Base APK Cache
    needs: [download-apks, build-apps]
    if: ${{ !cancelled() && needs.build-apps.result != 'skipped' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Install Python
        uses: actions/setup-python@v7
        with:
          python-version: 3.11

      - name: Download Shared Base APK Inputs
        id: shared-inputs
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          path: base-input-artifacts
          pattern: base-input-*

      - name: Download Build Results for Cache Promotion
        id: build-results
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          path: build-results
          pattern: build-status-*
          merge-multiple: true

      - name: Stage Verified Cache Candidates from Shared Inputs
        if: steps.shared-inputs.outcome == 'success' && steps.build-results.outcome == 'success'
        env:
          BASE_APK_CACHE_DIR: base-apk-cache-in
        run: |
          pip install -r requirements.txt
          python3 -m scripts.stage_shared_base_apk_cache

      - name: Update Hidden Durable Cache
        if: hashFiles('base-apk-cache-in/*') != ''
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          BASE_APK_CACHE_TAG: 'base-apk-cache-v2'
          BASE_APK_CACHE_KEEP: '10'
        run: python3 -m scripts.update_base_apk_cache

  create-release:
    name: Create Integrated Release
    needs: [download-tools, prepare-matrix, build-apps, scan-base-apks]
    runs-on: ubuntu-latest
    timeout-minutes: 300
    if: >-
      ${{
        !cancelled() &&
        needs.download-tools.result == 'success' &&
        needs.prepare-matrix.result == 'success' &&
        needs.build-apps.result != 'skipped' &&
        needs.scan-base-apks.result == 'success'
      }}
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: 3.11

      - name: Download All APK Artifacts
        uses: actions/download-artifact@v8
        with:
          path: ./all-apks
          pattern: apk-*

      - name: Download Build Results
        if: always()
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          path: ./build-results
          pattern: build-status-*
          merge-multiple: true

      - name: Collect All APKs
        run: python3 scripts/prepare_release_assets.py

      - name: Download VirusTotal Report
        uses: actions/download-artifact@v8
        with:
          path: .
          name: virustotal-report

      - name: Check if Any APKs Were Built
        id: check-apks
        env:
          EXPECTED_MATRIX: ${{ needs.prepare-matrix.outputs.matrix }}
        run: python3 scripts/check_release_assets.py

      - name: Add VirusTotal Report to Job Summary
        if: always() && hashFiles('virustotal_base_results.md') != ''
        run: cat virustotal_base_results.md >> "$GITHUB_STEP_SUMMARY"

      - name: Generate Release Tag (JST)
        if: steps.check-apks.outputs.skip_release == 'false'
        id: tag
        run: python3 scripts/generate_release_tag.py

      - name: Generate Release Notes
        if: steps.check-apks.outputs.skip_release == 'false'
        env:
          EXPECTED_MATRIX: ${{ needs.prepare-matrix.outputs.matrix }}
          SOURCE_TAGS_JSON: ${{ needs.download-tools.outputs.resolved-tags }}
          MORPHE_UPDATED: ${{ github.event.inputs.morphe_updated }}
          ANDDEA_UPDATED: ${{ github.event.inputs.anddea_updated }}
          RUSHIRANPISE_UPDATED: ${{ github.event.inputs.rushiranpise_updated }}
          ROOKIE_UPDATED: ${{ github.event.inputs.rookie_updated }}
          TOSOX_UPDATED: ${{ github.event.inputs.tosox_updated }}
          YUZU_UPDATED: ${{ github.event.inputs.yuzu_updated }}
          DROPPED_UPDATED: ${{ github.event.inputs.dropped_updated }}
          MORPHE_FORCE: ${{ github.event.inputs.morphe_force_build }}
          ANDDEA_FORCE: ${{ github.event.inputs.anddea_force_build }}
          RUSHIRANPISE_FORCE: ${{ github.event.inputs.rushiranpise_force_build }}
          ROOKIE_FORCE: ${{ github.event.inputs.rookie_force_build }}
          TOSOX_FORCE: ${{ github.event.inputs.tosox_force_build }}
          YUZU_FORCE: ${{ github.event.inputs.yuzu_force_build }}
          DROPPED_FORCE: ${{ github.event.inputs.dropped_force_build }}
        run: |
          python3 -m scripts.release_notes

      - name: Create Release
        if: steps.check-apks.outputs.skip_release == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ steps.tag.outputs.release_tag }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: python3 -m scripts.create_release

      - name: Display Release Summary
        if: steps.check-apks.outputs.skip_release == 'false'
        env:
          RELEASE_TAG: ${{ steps.tag.outputs.release_tag }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: python3 scripts/display_release_summary.py

  persist-successful-state:
    name: Persist Successful Upstream State
    needs: [download-tools, build-apps, create-release]
    if: >-
      ${{
        !cancelled() &&
        needs.build-apps.result == 'success' &&
        needs.create-release.result == 'success'
      }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.11'
          cache: pip

      - name: Verify Every Matrix Build Succeeded
        uses: actions/download-artifact@v8
        with:
          path: ./build-results
          pattern: build-status-*
          merge-multiple: true

      - name: Refuse Partial State Persistence
        run: |
          if [ ! -d ./build-results ] || ! find ./build-results -name '*.txt' -print -quit | grep -q .; then
            echo "::error::Per-app build results are missing; refusing to advance successful state."
            exit 1
          fi
          if grep -R -E '^status=(failure|cancelled|skipped)$' ./build-results; then
            echo "::error::At least one matrix build failed; refusing to advance successful state."
            exit 1
          fi

      - name: Close resolved auto-generated build issues
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: python3 scripts/close_resolved_build_issues.py --directory ./build-results

      - name: Save Resolved Patch and APK Versions
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SOURCE_TAGS_JSON: ${{ needs.download-tools.outputs.resolved-tags }}
        run: |
          pip install -r requirements.txt
          python3 scripts/save_successful_state.py

      - name: Commit State When Changed
        run: |
          if git diff --quiet -- last-tags.json; then
            echo "State already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -- last-tags.json
          git commit -m "chore: record successful build state [skip ci]"
          git push

  handle-build-failure:
    name: Open or Update Build Failure Issue
    needs: [download-tools, prepare-matrix, download-apks, build-apps, create-release, report-build-failures]
    if: >-
      ${{
        always() &&
        needs.report-build-failures.result != 'success' &&
        (needs.download-tools.result == 'failure' ||
         needs.prepare-matrix.result == 'failure' ||
         needs.download-apks.result == 'failure' ||
         needs.build-apps.result == 'failure' ||
         needs.create-release.result == 'failure')
      }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write

    steps:
      - name: Open or Update Failure Issue
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          TOOLS_RESULT: ${{ needs.download-tools.result }}
          MATRIX_RESULT: ${{ needs.prepare-matrix.result }}
          DOWNLOAD_RESULT: ${{ needs.download-apks.result }}
          BUILD_RESULT: ${{ needs.build-apps.result }}
          RELEASE_RESULT: ${{ needs.create-release.result }}
          REPORT_RESULT: ${{ needs.report-build-failures.result }}
        run: |
          title="Automated APK build workflow failed"
          cat <<EOF > build-failure-issue.md
          # Automated APK Build Failure Report

          An error occurred during the APK build and release workflow run.

          ## Job Status Summary
          - **Download Tools**: ${TOOLS_RESULT}
          - **Prepare Matrix**: ${MATRIX_RESULT}
          - **Download APKs**: ${DOWNLOAD_RESULT}
          - **Build Apps**: ${BUILD_RESULT}
          - **Create Release**: ${RELEASE_RESULT}
          - **Detailed Reporter**: ${REPORT_RESULT}

          **Workflow Run**: ${RUN_URL}
          EOF

          issue=$(gh issue list --state open --search "$title in:title" --json number,title \
            --jq ".[] | select(.title == \"$title\") | .number" | head -n 1)
          if [ -n "$issue" ]; then
            gh issue comment "$issue" --body-file build-failure-issue.md
          else
            gh issue create --title "$title" --body-file build-failure-issue.md --label "bug"
          fi
